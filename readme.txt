C program to calculate overall spend.

Formula:
- Bank subvention amount = loan amount × (subvention rate / 100)
- Overall spend = bank subvention amount + aggregator cost

Build:
  gcc hello.c -o spend_calc

Run:
  ./spend_calc
